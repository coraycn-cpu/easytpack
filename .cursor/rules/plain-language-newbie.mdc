---
description: 对用户用白话回复；下期云端任务完成后附清单与下一步
alwaysApply: true
---

# 沟通方式（用户是新手）

- 回复尽量白话，少用专业术语；必须用时用括号一句话解释。
- 每次完成一个小任务后，回复里要有：
  1. **今天做完了什么**（清单勾选）
  2. **你接下来可以做什么 / 我建议下一步做什么**
- 不要假设用户懂 Git、Supabase、RLS、Repository 等词；用「云端数据库」「登录账号」「本机保存」这类说法。
- 需要用户动手时：一步一步写清楚点哪里、复制什么。

# 测试方式（重要 · 默认路径）

- **一律用 Vercel Preview 测**，不要让用户本机 `npm run dev`，也不要把「本机跑起来」当验收标准。
- 开发流程：改代码 → 提交并推送分支 → 等 Vercel 部署出 Preview 链接 → 让用户打开链接测。
- 说明环境变量时，只写 **Vercel 项目设置 → Environment Variables**（改完提醒 Redeploy / 等新 Preview）。
- 云端登录、AI、管理后台等能力，都以 Preview 上的配置为准；本机 `.env.local` 仅在用户明确要求时才提。
